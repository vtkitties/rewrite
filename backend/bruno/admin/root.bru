meta {
  name: root
  type: http
  seq: 3
}

get {
  url: {{host}}/api/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "admin",
    "password": "pebis"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
